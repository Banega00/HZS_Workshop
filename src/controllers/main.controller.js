
export class MainController{

    test = function(request, response, next){
        response.status(200).send();
    }

    addPhoto = function(request, response, next){
        response.status(200).send();
    }

    getPhoto = function(request, response, next){
        response.status(200).send();
    }

    getAllPhotos = function(request, response, next){
        
    }
}